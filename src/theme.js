'use client';

import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#B0733E',
      border: '10px' // Defina a cor primária
    },
    secondary: {
      main: '#F4A261', // Defina a cor secundária
    },
    
  },
});


export default theme;
